<div class="center flex-column m-3">
  <%= link_to "Create Post", "/users/:user_id/posts/new", class: 'btn-primary fixed-top text-center p-2' %>
  <%= render partial: "shared/user", locals: {user: @user}%>
  <div class="w-50 w-100-mobile mb-3 bg-light border border-secondary border-2 p-2">
    <h3>Bio</h3>
    <p><%= @user.bio %></p>
  </div>
  <div class="d-flex flex-column w-50 w-100-mobile">
    <% @user.recent_posts.each do |post| %>
      <div class="d-flex flex-column bg-light p-2 border mb-3 border border-secondary border-2">
        <h5><%= link_to post.title, user_post_path(@user, post.id), class: "no-style" %></h5>
        <p><%= post.text %></p>
        <p class="small align-self-end">
          Comments
          <span class="badge bg-success">
            <%= post.comments_counter %>
          </span>
          Likes
          <span class="badge bg-warning text-dark">
            <%= post.likes_counter %>
          </span>
        </p>
      </div>
    <% end %>
  </div>
  <%= link_to "See all posts" , user_posts_path(@user.id), class: "btn btn-primary" %>
</div>