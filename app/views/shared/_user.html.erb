<div class="d-flex w-50 w-100-mobile mb-3 mt-5 border border-secondary border-2 p-2">
    <img class='user-image' src=<%= user.photo %> />
    <div class="d-flex flex-column align-items-end justify-content-center w-100 bg-light p-2">
    <h2 class="align-self-start"><%= user.name %></h2>
    <p>Number of posts
      <span class="badge bg-primary">
        <%= user.posts_counter %>
      <span>
    </p>
  </div>
</div>